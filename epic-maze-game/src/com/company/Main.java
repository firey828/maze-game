package com.company;

import com.company.essentials.AlienOverlord;

public class Main {

    public static void main(String[] args) {
        AlienOverlord y = new AlienOverlord();
        /*
        int[][] board = new int[10][10];
        // 0 = player
        // 1 = regular threshold
        // 2 = torch threshold
        // 3 = map threshold
        for (int x = 0; x < board.length; x++) {
            for (int y = 0; y < board[x].length; y++) {
                board[x][y] = 3;
            }
        }

        board[4][2] = 0;

        int currentRow = 4;
        int currentCol = 2;

        for (int x = 0; x < board.length; x++) {
            for (int y = 0; y < board[x].length; y++) {
                if (x == currentRow - 1 || x == currentRow || x == currentRow + 1) {
                    if (y == currentCol - 1 || y == currentCol || y == currentCol + 1) {
                        board[x][y] = 1;
                    }
                }
            }
        }

        String msg = "";

        for (int x = 0; x < board.length; x++) {
            for (int y = 0; y < board[x].length; y++) {
                msg += board[x][y] + " ";
            }
            msg += "\n";
        }

        System.out.println(msg);
        */
    }
}
